node_modules
dist
.DS_Store
*.log
__pycache__/
*.pyc
__pycache__/
*.pyc

# local LLM runtime usage ledger; commit only example JSONL files
public/data/v2/ops/llm_usage.jsonl

# auto-work-mode run state (per-shift, never commit)
.shifts/

# multi-agent task queue (per-agent state, never commit)
.agent_tasks/
.agent_tasks/design/
data_history/

# v3 research raw per-window backtest outputs — bulky intermediate data.
# Mainline keeps only canonical *_bundle.json + verdicts/reports (Junyan
# 2026-05-29 Bundle G). Per-window wf_*/full_* and iter raw JSONs stay local.
public/data/v3c_alpha1*_wf_*.json
public/data/v3c_alpha1*_full_*.json
public/data/iter1*_backtest_*.json
public/data/iter1*_bootstrap_*.json

# R-015 事件账本的 flock 句柄与原子写临时文件:运行时产物,不入库
experiments/execution_tracker/*.jsonl.lock
experiments/execution_tracker/*.anchor.json.tmp
